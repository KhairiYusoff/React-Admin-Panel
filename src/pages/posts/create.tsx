import {
    Create,
    Form,
    Input,
    Select,
    useForm,
    useSelect
} from "@pankod/refine-antd"
import { IPost } from "interfaces"

export const PostCreate: React.FC = () => {
    return <div></div>
}